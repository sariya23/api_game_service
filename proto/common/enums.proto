syntax = "proto3";

package game.common;
option go_package = "github.com/sariya23/api_game_service/gen/common;common";



enum GameStatusType {
  UNKNOWN = 0;
  DRAFT = 1; // Игра в драфте
  PENDING = 2; // Игра валидируется
  PUBLISH = 3; // Игра опубликована
}